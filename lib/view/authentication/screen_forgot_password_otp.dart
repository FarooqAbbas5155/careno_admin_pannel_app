import 'package:flutter/material.dart';

class ScreenForgotPasswordOtp extends StatelessWidget {
  const ScreenForgotPasswordOtp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
